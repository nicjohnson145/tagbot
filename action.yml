name: TagBot
description: 'Automatically create tags'
outputs:
  tag:
    description: 'Id of the newly created tag, if tag was created'
runs:
  using: docker
  image: ghcr.io/nicjohnson145/tagbot:latest
  args:
  - "--debug"
